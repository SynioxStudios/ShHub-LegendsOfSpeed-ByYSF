local SynioxLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/SynioxStudios/ByYSF-FLuent-U--V4/refs/heads/main/SynioxUi.txt"))()
local displayName = game.Players.LocalPlayer.DisplayName

SynioxLib:StartFlowRGB(
    Color3.fromRGB(120, 0, 0),
    Color3.fromRGB(40, 0, 0),
    Color3.fromRGB(0, 0, 0)
)

local Window = SynioxLib:CreateWindow({
    Title = "Syniox Hub | LoS || HELLO - " .. displayName,
    SubTitle = "v4",
    TabWidth = 160,
    Size = UDim2.fromOffset(470, 350),
    Acrylic = true,
    Theme = "SynioxHub"
})

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local player = game.Players.LocalPlayer

local MainFarm = Window:AddTab({ Title = "Main", Icon = "home" })

local selectedOrb = ""
local selectedMap = ""
local farming = false
local autoRace = false
local hoopConnection

MainFarm:AddDropdown("SelectMap", {
    Title = "📍 Select Map",
    Values = {"City", "Snow City", "Magma City", "Speed Jungle", "Legends Highway"},
    Default = "",
    Callback = function(Value)
        selectedMap = Value
    end
})

MainFarm:AddDropdown("SelectOrb", {
    Title = "💎 Select Orb",
    Values = {"Yellow Orb", "Red Orb", "Blue Orb", "Orange Orb", "Ethereal Orb", "Gem"},
    Default = "",
    Callback = function(Value)
        selectedOrb = Value
    end
})

MainFarm:AddToggle("AutoFarmOrbs", {
    Title = "Auto Farm Orbs",
    Default = false,
    Callback = function(state)
        farming = state
        if farming then
            task.spawn(function()
                while farming do
                    if selectedOrb ~= "" and selectedMap ~= "" then
                        ReplicatedStorage.rEvents.orbEvent:FireServer("collectOrb", selectedOrb, selectedMap)
                    end
                    task.wait(0.001)
                end
            end)
        end
    end
})

MainFarm:AddToggle("AutoHoop", {
    Title = "⬆️ Auto Hoop",
    Default = false,
    Callback = function(state)
        if state then
            hoopConnection = RunService.RenderStepped:Connect(function()
                local root = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
                local hoops = game.Workspace:FindFirstChild("Hoops")
                if root and hoops then
                    for _, hoop in ipairs(hoops:GetChildren()) do
                        if hoop.Name == "Hoop" or hoop:FindFirstChild("Apperance") then
                            hoop.CFrame = root.CFrame
                            ReplicatedStorage.rEvents.hoopEvent:FireServer("finishedHoop")
                        end
                    end
                end
            end)
        else
            if hoopConnection then
                hoopConnection:Disconnect()
                hoopConnection = nil
            end
        end
    end
})

MainFarm:AddToggle("AutoRace", {
    Title = "🏁 Auto Race/Win",
    Default = false,
    Callback = function(state)
        autoRace = state
    end
})

task.spawn(function()
    while true do
        task.wait(0.5)
        if autoRace then
            pcall(function()
                if player.PlayerGui.gameGui.raceJoinLabel.Visible then
                    ReplicatedStorage.rEvents.raceEvent:FireServer("joinRace")
                end
                local raceMaps = game.Workspace:FindFirstChild("raceMaps")
                if raceMaps and player.currentMap.Value ~= "" then
                    for _, map in ipairs(raceMaps:GetChildren()) do
                        if player.currentMap.Value == map.mapValue.Value then
                            repeat task.wait() until player.PlayerGui.gameGui.countdownLabels.goLabel.Visible or not autoRace
                            if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                                player.Character.HumanoidRootPart.CFrame = map.finishPart.CFrame
                            end
                        end
                    end
                end
            end)
        end
    end
end)

local RebirthTab = Window:AddTab({ Title = "Rebirths", Icon = "refresh-cw" })
local targetRebirthValue = 0

RebirthTab:AddInput("RebirthTarget", {
    Title = "Rebirth Target",
    Default = "0",
    Numeric = true,
    Finished = true,
    Callback = function(Value)
        targetRebirthValue = tonumber(Value) or 0
    end
})

RebirthTab:AddToggle("AutoRebirthTarget", {
    Title = "🔄 Auto Rebirth Target",
    Default = false,
    Callback = function(bool)
        _G.targetRebirthActive = bool
        if bool then
            task.spawn(function()
                while _G.targetRebirthActive do
                    local currentRebirths = game.Players.LocalPlayer.leaderstats.Rebirths.Value
                    if currentRebirths >= targetRebirthValue then
                        _G.targetRebirthActive = false
                        break
                    end
                    game:GetService("ReplicatedStorage").rEvents.rebirthEvent:FireServer("rebirthRequest")
                    task.wait(0.1)
                end
            end)
        end
    end
})

RebirthTab:AddToggle("AutoRebirthInf", {
    Title = "♾️ Auto Rebirth (Infinitely)",
    Default = false,
    Callback = function(bool)
        _G.infiniteRebirthActive = bool
        if bool then
            task.spawn(function()
                while _G.infiniteRebirthActive do
                    game:GetService("ReplicatedStorage").rEvents.rebirthEvent:FireServer("rebirthRequest")
                    task.wait(0.1)
                end
            end)
        end
    end
})

local PetsTab = Window:AddTab({ Title = "Pets & Aura", Icon = "shopping-cart" })

PetsTab:AddSection("💎 Crystals")

local selectedCrystal = ""
PetsTab:AddDropdown("CryDrop", { 
    Title = "Select Crystal", 
    Values = {"Blue Crystal", "Red Crystal", "Lightning Crystal", "Inferno Crystal", "Lava Crystal", "Snow Crystal", "Electro Crystal", "Desert Crystal"}, 
    Default = "", 
    Callback = function(v) 
        selectedCrystal = v 
    end 
})

PetsTab:AddToggle("AutoCry", {
    Title = "🥚 Auto Open Crystal",
    Default = false,
    Callback = function(bool)
        _G.AutoHatchCry = bool
        if bool then
            task.spawn(function()
                while _G.AutoHatchCry do
                    ReplicatedStorage.rEvents.openCrystalRemote:InvokeServer("openCrystal", selectedCrystal)
                    task.wait(0.1)
                end
            end)
        end
    end
})

PetsTab:AddSection("🐢 Pets")

local selectedPet = ""
PetsTab:AddDropdown("PetDrop", { 
    Title = "Select Pet", 
    Values = {"None", "Red Bunny", "Red Kitty", "Green Vampy", "Blue Bunny", "Dark Golem", "Silver Dog", "Pink Butterfly", "Purple Pegasus", "Yellow Squeak", "Yellow Butterfly", "Green Golem", "Golden Angel", "Orange Pegasus", "Golden Pheonix", "Orange Falcon", "Green Firecaster", "Blue Firecaster", "White Pheonix", "Red Pheonix", "Red Firecaster", "Flaming Hedgehog", "Quantum Dragon", "Tundra Dragon", "Magic Butterfly", "Soul Fusion Dog", "Hypersonic Pegasus", "Dark Soul Birdie", "Eternal Nebula Dragon", "Shadows Edge Kitty", "Ultimate Overdrive Bunny", "Blue Pheonix", "Magical Pegasus", "Voltaic Falcon", "Electro Golem", "Maestro Dog", "Divine Pegasus", "Golden Viking", "Speedy Sensei", "Swift Samurai"}, 
    Default = "", 
    Callback = function(v) 
        selectedPet = v 
    end 
})

PetsTab:AddToggle("AutoPet", {
    Title = "🐾 Auto Open Pet",
    Default = false,
    Callback = function(bool)
        _G.AutoHatchPet = bool
        if bool then
            task.spawn(function()
                while _G.AutoHatchPet do
                    if selectedPet ~= "" and selectedPet ~= "None" then
                        local petFolder = ReplicatedStorage:FindFirstChild("cPetShopFolder")
                        local petToOpen = petFolder and petFolder:FindFirstChild(selectedPet)
                        if petToOpen then
                            ReplicatedStorage.cPetShopRemote:InvokeServer(petToOpen)
                        end
                    end
                    task.wait(0.1)
                end
            end)
        end
    end
})

PetsTab:AddSection("✨ Aura")

local selectedAura = "None"
PetsTab:AddDropdown("AuraDrop", { 
    Title = "Select Aura", 
    Values = {"None", "Hyperblast", "1st Trail", "Dragon Fire", "Yellow Trail", "Rainbow Steps", "Rainbow Speed"}, 
    Default = "None", 
    Callback = function(v) 
        selectedAura = v 
    end 
})

PetsTab:AddToggle("AutoAura", {
    Title = "🔥 Auto Open Aura",
    Default = false,
    Callback = function(bool)
        _G.AutoHatchAura = bool
        if bool then
            task.spawn(function()
                while _G.AutoHatchAura do
                    if selectedAura ~= "" and selectedAura ~= "None" then
                        local auraToOpen = ReplicatedStorage.cPetShopFolder:FindFirstChild(selectedAura)
                        if auraToOpen then
                            ReplicatedStorage.cPetShopRemote:InvokeServer(auraToOpen)
                        end
                    end
                    task.wait(0.1)
                end
            end)
        end
    end
})

local TeleportTab = Window:AddTab({ Title = "Teleport", Icon = "map-pin" })

TeleportTab:AddButton({
    Title = "🏙️ City",
    Description = "X: -753, Y: 4, Z: 549",
    Callback = function()
        player.Character.HumanoidRootPart.CFrame = CFrame.new(-753.82, 4.19, 549.09)
    end
})

TeleportTab:AddButton({
    Title = "🛣️ Legends Highway",
    Description = "X: 3737, Y: 71, Z: 5593",
    Callback = function()
        player.Character.HumanoidRootPart.CFrame = CFrame.new(3737.63, 71.72, 5593.70)
    end
})

TeleportTab:AddSection("🏙️ Spawn City's")

TeleportTab:AddButton({
    Title = "❄️ Snow City",
    Callback = function()
        player.Character.HumanoidRootPart.CFrame = CFrame.new(-866.39, 4.19, 2165.71)
    end
})

TeleportTab:AddButton({
    Title = "🌋 Magma City",
    Callback = function()
        player.Character.HumanoidRootPart.CFrame = CFrame.new(1616.83, 4.24, 4330.65)
    end
})

TeleportTab:AddButton({
    Title = "🌳 Speed Jungle",
    Callback = function()
        player.Character.HumanoidRootPart.CFrame = CFrame.new(1920.05, 3.11, 12943.64)
    end
})

local LookDura = Window:AddTab({ Title = "Stats", Icon = "bar-chart-2" })
local SelectPlayerName = ""

local PlayerDrop = LookDura:AddDropdown("SelectPlayer", {
    Title = "Select Player",
    Values = {},
    Default = nil,
    Callback = function(Value)
        if Value then
            SelectPlayerName = Value:match("| (.+)") or Value
        end
    end
})

local function UpdatePlayerList()
    local players = {}
    for _, Plr in pairs(game:GetService("Players"):GetPlayers()) do
        table.insert(players, Plr.DisplayName .. " | " .. Plr.Name)
    end
    PlayerDrop:SetValues(players)
end

UpdatePlayerList()
game.Players.PlayerAdded:Connect(UpdatePlayerList)
game.Players.PlayerRemoving:Connect(UpdatePlayerList)

local function FormatNumberWithCommas(number)
    local formatted = tostring(number):reverse():gsub("(%d%d%d)", "%1,"):reverse()
    return formatted:gsub("^,", "")
end

local function FormatAbbreviated(number)
    local abbreviations = {"", "K", "M", "B", "T", "Qa", "Qi", "Sx", "Sp"}
    local abbreviationIndex = 1
    while number >= 1000 and abbreviationIndex < #abbreviations do
        number = number / 1000
        abbreviationIndex = abbreviationIndex + 1
    end
    return string.format("%.2f", number) .. abbreviations[abbreviationIndex]
end

local function FormatDisplay(value)
    local normal = FormatNumberWithCommas(value)
    local abbreviated = FormatAbbreviated(value)
    return normal .. " (" .. abbreviated .. ")"
end

local StatDisplay = LookDura:AddParagraph({
    Title = "Player Stats",
    Content = "Select a player to view data"
})

task.spawn(function()
    while task.wait(0.5) do
        if SelectPlayerName ~= "" then
            local target = game.Players:FindFirstChild(SelectPlayerName)
            if target then
                local lstats = target:FindFirstChild("leaderstats")
                local steps = "0"
                local rebirths = "0"
                local gems = "0"
                local level = "0"

                if lstats then
                    if lstats:FindFirstChild("Steps") then steps = FormatDisplay(lstats.Steps.Value) end
                    if lstats:FindFirstChild("Rebirths") then rebirths = FormatDisplay(lstats.Rebirths.Value) end
                end
                
                if target:FindFirstChild("Gems") then gems = FormatDisplay(target.Gems.Value) end
                if target:FindFirstChild("level") then level = FormatDisplay(target.level.Value) end

                StatDisplay:SetTitle("📊 Stats for: " .. target.Name)
                StatDisplay:SetDesc(
                    "⚡ Steps: " .. steps .. "\n" ..
                    "💎 Gems: " .. gems .. "\n" ..
                    "🔄 Rebirths: " .. rebirths .. "\n" ..
                    "📈 Level: " .. level
                )
            end
        end
    end
end)

local Misc = Window:AddTab({ Title = "Misc", Icon = "settings" })

Misc:AddToggle("AntiAfk", {
    Title = "⏳ Anti Afk",
    Default = false,
    Callback = function(Value)
        if Value then
            _G.AntiAfkActive = true
            local vu = game:GetService("VirtualUser")
            antiAfkConnection = game:GetService("Players").LocalPlayer.Idled:Connect(function()
                if _G.AntiAfkActive then
                    vu:CaptureController()
                    vu:ClickButton2(Vector2.new())
                end
            end)

            local ScreenGui = Instance.new("ScreenGui")
            local MainFrame = Instance.new("Frame")
            local UICorner = Instance.new("UICorner")
            local Title = Instance.new("TextLabel")
            local Separator = Instance.new("Frame")
            local TimerLabel = Instance.new("TextLabel")
            local FPSLabel = Instance.new("TextLabel")
            local MSLabel = Instance.new("TextLabel")
            local UIStroke = Instance.new("UIStroke")

            ScreenGui.Name = "Syniox_AntiAFK"
            ScreenGui.Parent = game:GetService("CoreGui")
            
            MainFrame.Parent = ScreenGui
            MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0) 
            MainFrame.BackgroundTransparency = 0.1
            MainFrame.Position = UDim2.new(0.05, 0, 0.4, 0)
            MainFrame.Size = UDim2.new(0, 120, 0, 125)
            MainFrame.Active = true
            MainFrame.Draggable = true 

            UICorner.CornerRadius = UDim.new(0, 8)
            UICorner.Parent = MainFrame

            UIStroke.Parent = MainFrame
            UIStroke.Color = Color3.fromRGB(150, 0, 0) 
            UIStroke.Thickness = 1.5

            Title.Parent = MainFrame
            Title.Size = UDim2.new(1, 0, 0, 28)
            Title.Text = "Syniox Anti-Afk" 
            Title.TextColor3 = Color3.fromRGB(200, 0, 0)
            Title.BackgroundTransparency = 1
            Title.TextSize = 15
            Title.Font = Enum.Font.SourceSansBold

            Separator.Parent = MainFrame
            Separator.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
            Separator.BorderSizePixel = 0
            Separator.Position = UDim2.new(0.1, 0, 0, 28)
            Separator.Size = UDim2.new(0.8, 0, 0, 1)

            local function SetupLabel(lbl, text, pos)
                lbl.Parent = MainFrame
                lbl.Position = pos
                lbl.Size = UDim2.new(1, 0, 0, 25)
                lbl.Text = text
                lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
                lbl.BackgroundTransparency = 1
                lbl.TextSize = 16
                lbl.Font = Enum.Font.SourceSansBold
                lbl.TextXAlignment = Enum.TextXAlignment.Center
            end

            SetupLabel(TimerLabel, "⏳ Time: 00:00:00", UDim2.new(0, 0, 0, 35))
            SetupLabel(FPSLabel, "🚀 FPS: 0", UDim2.new(0, 0, 0, 62))
            SetupLabel(MSLabel, "📡 MS: 0", UDim2.new(0, 0, 0, 89))

            local startTime = os.time()
            local RunService = game:GetService("RunService")
            local Stats = game:GetService("Stats")
            
            task.spawn(function()
                while _G.AntiAfkActive do
                    local diff = os.time() - startTime
                    TimerLabel.Text = string.format("⏳ Time: %02d:%02d:%02d", math.floor(diff/3600), math.floor((diff%3600)/60), diff%60)
                    local fps = math.floor(1 / RunService.RenderStepped:Wait())
                    FPSLabel.Text = "🚀 FPS: " .. fps
                    local ping = math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue())
                    MSLabel.Text = "📡 MS: " .. ping
                    task.wait(0.5)
                end
            end)
        else
            _G.AntiAfkActive = false
            if antiAfkConnection then antiAfkConnection:Disconnect() end
            local oldGui = game:GetService("CoreGui"):FindFirstChild("Syniox_AntiAFK")
            if oldGui then oldGui:Destroy() end
        end
    end
})

local TeleportTarget = ""
local CurrentTPList = {}

local PlayerTPDropdown = Misc:AddDropdown("PlayerTPDropdown", {
    Title = "🎯 Select Player",
    Values = {},
    Default = nil,
    Callback = function(Value)
        TeleportTarget = Value
    end
})

local function AddPlayerToList(plr)
    if plr ~= player then
        table.insert(CurrentTPList, plr.DisplayName)
        PlayerTPDropdown:SetValues(CurrentTPList)
    end
end

for _, v in ipairs(game.Players:GetPlayers()) do
    AddPlayerToList(v)
end

game.Players.PlayerAdded:Connect(function(newPlr)
    AddPlayerToList(newPlr)
end)

game.Players.PlayerRemoving:Connect(function(oldPlr)
    for i, name in ipairs(CurrentTPList) do
        if name == oldPlr.DisplayName then
            table.remove(CurrentTPList, i)
            break
        end
    end
    PlayerTPDropdown:SetValues(CurrentTPList)
end)

Misc:AddButton({
    Title = "🚀 Teleport to Player",
    Callback = function()
        if TeleportTarget ~= "" then
            for _, v in ipairs(game.Players:GetPlayers()) do
                if v.DisplayName == TeleportTarget and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = v.Character.HumanoidRootPart.CFrame
                    break
                end
            end
        end
    end
})

local spyTargetDropdown = Misc:AddDropdown("SpyTargetDropdown", {
    Title = "👁️ Select View",
    Values = {},
    Default = nil,
    Callback = function(Value)
        _G.TargetPlayerName = Value
    end
})

local function UpdateDropdown()
    local players = {}
    for _, v in ipairs(game.Players:GetPlayers()) do
        if v ~= player then
            table.insert(players, v.Name)
        end
    end
    spyTargetDropdown:SetValues(players)
end

UpdateDropdown()

game.Players.PlayerAdded:Connect(UpdateDropdown)
game.Players.PlayerRemoving:Connect(UpdateDropdown)

Misc:AddToggle("ViewPlayerToggle", {
    Title = "👀 View Player",
    Default = false,
    Callback = function(bool)
        _G.Spying = bool
        if not bool then
            workspace.CurrentCamera.CameraSubject = player.Character:FindFirstChild("Humanoid")
            return
        end
        task.spawn(function()
            while _G.Spying do
                if _G.TargetPlayerName then
                    local target = game.Players:FindFirstChild(_G.TargetPlayerName)
                    if target and target.Character then
                        local humanoid = target.Character:FindFirstChild("Humanoid")
                        if humanoid then
                            workspace.CurrentCamera.CameraSubject = humanoid
                        end
                    end
                end
                task.wait(0.1)
            end
        end)
    end
})

Misc:AddSection("Character Settings")

local walkSpeedValue = 200
Misc:AddInput("WSInput", {
    Title = "🏃 Walk Speed",
    Default = "200",
    Numeric = true,
    Finished = true,
    Callback = function(Value)
        walkSpeedValue = tonumber(Value) or 16
    end
})

Misc:AddToggle("EnableWalkSpeed", {
    Title = "✅ Enable Walk Speed",
    Default = false,
    Callback = function(state)
        _G.WSLoop = state
        task.spawn(function()
            while _G.WSLoop do
                if player.Character and player.Character:FindFirstChild("Humanoid") then
                    player.Character.Humanoid.WalkSpeed = walkSpeedValue
                end
                task.wait()
            end
        end)
    end
})

local jumpPowerValue = 250
Misc:AddInput("JPInput", {
    Title = "🚀 Jump Power",
    Default = "250",
    Numeric = true,
    Finished = true,
    Callback = function(Value)
        jumpPowerValue = tonumber(Value) or 50
    end
})

Misc:AddToggle("EnableJumpPower", {
    Title = "✅ Enable Jump Power",
    Default = false,
    Callback = function(state)
        _G.JPLoop = state
        task.spawn(function()
            while _G.JPLoop do
                if player.Character and player.Character:FindFirstChild("Humanoid") then
                    player.Character.Humanoid.UseJumpPower = true
                    player.Character.Humanoid.JumpPower = jumpPowerValue
                end
                task.wait()
            end
        end)
    end
})

Misc:AddSection("Other Settings")

Misc:AddToggle("LockPos", {
    Title = "🔒 Lock Position",
    Default = false,
    Callback = function(Value)
        if Value then
            local currentPos = player.Character.HumanoidRootPart.CFrame
            getgenv().posLock = game:GetService("RunService").Heartbeat:Connect(function()
                if player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = currentPos
                end
            end)
        else
            if getgenv().posLock then
                getgenv().posLock:Disconnect()
                getgenv().posLock = nil
            end
        end
    end
})

Misc:AddToggle("AutoFortune", {
    Title = "🎰 Auto Fortune Wheel",
    Default = false,
    Callback = function(Value)
        _G.autoFortuneWheelActive = Value
        if Value then
            task.spawn(function()
                while _G.autoFortuneWheelActive do
                    local args = {
                        [1] = "openFortuneWheel",
                        [2] = ReplicatedStorage:WaitForChild("fortuneWheelChances"):WaitForChild("Fortune Wheel")
                    }
                    ReplicatedStorage:WaitForChild("rEvents"):WaitForChild("openFortuneWheelRemote"):InvokeServer(unpack(args))
                    task.wait(0.1)
                end
            end)
        end
    end
})

Misc:AddSection("Optimization")

Misc:AddButton({
    Title = "🚀 FPS Boost",
    Description = "Removes textures and shadows to increase FPS",
    Callback = function()
        local terrain = game.Workspace:FindFirstChildOfClass('Terrain')
        terrain.WaterWaveSize = 0
        terrain.WaterWaveSpeed = 0
        terrain.WaterReflectance = 0
        terrain.WaterTransparency = 0
        game:GetService("Lighting").GlobalShadows = false
        game:GetService("Lighting").FogEnd = 9e9
        settings().Rendering.QualityLevel = 1
        for _, v in pairs(game:GetDescendants()) do
            if v:IsA("Part") or v:IsA("UnionOperation") or v:IsA("MeshPart") or v:IsA("CornerWedgePart") or v:IsA("TrussPart") then
                v.Material = Enum.Material.Plastic
                v.Reflectance = 0
            elseif v:IsA("Decal") or v:IsA("Texture") then
                v:Destroy()
            elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                v.Enabled = false
            elseif v:IsA("Explosion") then
                v.Visible = false
            end
        end
    end
})

Misc:AddToggle("WhiteScreen", {
    Title = "🖥️ CPU Boost (White Screen)",
    Default = false,
    Callback = function(bool)
        _G.WhiteScreen = bool
        if _G.WhiteScreen then
            game:GetService("RunService"):Set3dRenderingEnabled(false)
        else
            game:GetService("RunService"):Set3dRenderingEnabled(true)
        end
    end
})

Misc:AddToggle("AutoChest", {
    Title = "🎁 Auto Collect Chests",
    Default = false,
    Callback = function(state)
        _G.AutoChest = state
        task.spawn(function()
            while _G.AutoChest do
                for _, v in pairs(game.Workspace:GetChildren()) do
                    if v.Name:find("Chest") and v:FindFirstChild("TouchInterest") then
                        firetouchinterest(player.Character.HumanoidRootPart, v, 0)
                        firetouchinterest(player.Character.HumanoidRootPart, v, 1)
                    end
                end
                task.wait(5)
            end
        end)
    end
})

Misc:AddButton({
    Title = "🔄 Rejoin Game",
    Callback = function()
        local ScreenGui = Instance.new("ScreenGui")
        local Frame = Instance.new("Frame")
        local TextLabel = Instance.new("TextLabel")
        local YesButton = Instance.new("TextButton")
        local NoButton = Instance.new("TextButton")

        ScreenGui.Parent = game:GetService("CoreGui")
        ScreenGui.Name = "RejoinConfirm"

        Frame.Name = "MainFrame"
        Frame.Parent = ScreenGui
        Frame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
        Frame.Position = UDim2.new(0.5, -125, 0.5, -75)
        Frame.Size = UDim2.new(0, 250, 0, 150)
        Frame.BorderSizePixel = 0
        Frame.Active = true
        Frame.Draggable = true 
        Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 10)

        TextLabel.Parent = Frame
        TextLabel.BackgroundTransparency = 1
        TextLabel.Position = UDim2.new(0, 10, 0, 20)
        TextLabel.Size = UDim2.new(0, 230, 0, 50)
        TextLabel.Font = Enum.Font.GothamBold
        TextLabel.Text = "Are you sure you want to rejoin?"
        TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        TextLabel.TextSize = 16
        TextLabel.TextWrapped = true

        YesButton.Name = "YesButton"
        YesButton.Parent = Frame
        YesButton.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
        YesButton.Position = UDim2.new(0, 25, 0, 90)
        YesButton.Size = UDim2.new(0, 85, 0, 35)
        YesButton.Font = Enum.Font.GothamBold
        YesButton.Text = "YES"
        YesButton.TextColor3 = Color3.fromRGB(255, 255, 255)
        YesButton.TextSize = 14
        Instance.new("UICorner", YesButton).CornerRadius = UDim.new(0, 6)

        NoButton.Name = "NoButton"
        NoButton.Parent = Frame
        NoButton.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
        NoButton.Position = UDim2.new(0, 140, 0, 90)
        NoButton.Size = UDim2.new(0, 85, 0, 35)
        NoButton.Font = Enum.Font.GothamBold
        NoButton.Text = "NO"
        NoButton.TextColor3 = Color3.fromRGB(255, 255, 255)
        NoButton.TextSize = 14
        Instance.new("UICorner", NoButton).CornerRadius = UDim.new(0, 6)

        YesButton.MouseButton1Click:Connect(function()
            game:GetService("TeleportService"):Teleport(game.PlaceId, player)
        end)

        NoButton.MouseButton1Click:Connect(function()
            ScreenGui:Destroy()
        end)
    end
})

Misc:AddToggle("NoClip", {
    Title = "👻 No Clip",
    Default = false,
    Callback = function(bool)
        _G.noclipEnabled = bool
        game:GetService("RunService").Stepped:Connect(function()
            if _G.noclipEnabled and player.Character then
                for _, v in pairs(player.Character:GetDescendants()) do
                    if v:IsA("BasePart") then v.CanCollide = false end
                end
            end
        end)
    end
})
