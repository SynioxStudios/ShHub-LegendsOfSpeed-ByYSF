local SynioxLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/SynioxStudios/ByYSF-FLuent-U--V4/refs/heads/main/SynioxUi.txt"))()
local displayName = game.Players.LocalPlayer.DisplayName

SynioxLib:StartFlowRGB(
    Color3.fromRGB(120, 0, 0),
    Color3.fromRGB(40, 0, 0),
    Color3.fromRGB(0, 0, 0)
)

local Window = SynioxLib:CreateWindow({
    Title = "Syniox Hub | LoS || HELLO - " .. displayName,
    SubTitle = "v4",
    TabWidth = 160,
    Size = UDim2.fromOffset(450, 400),
    Acrylic = true,
    Theme = "SpeedHubX"
})

local MainFarm = Window:AddTab({ Title = "Main", Icon = "home" })
local RebirthTab = Window:AddTab({ Title = "Rebirths", Icon = "refresh-cw" })
local PetsTab = Window:AddTab({ Title = "Pets & Aura", Icon = "paw-print" })
local Misc = Window:AddTab({ Title = "Misc", Icon = "settings" })

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local player = game.Players.LocalPlayer

local selectedOrb = ""
local orbDropdown = MainFarm:AddDropdown("OrbDropdown", {
    Title = "💎 Select Orb",
    Values = {"Yellow Orb", "Red Orb", "Blue Orb", "Orange Orb", "Ethereal Orb", "Gem"},
    Default = "",
    Callback = function(Value)
        selectedOrb = Value
    end
})

MainFarm:AddToggle("AutoOrb", {
    Title = "🌕 Auto Farm Orbs",
    Default = false,
    Callback = function(state)
        _G.farming = state
        if _G.farming then
            task.spawn(function()
                while _G.farming do
                    if selectedOrb ~= "" then
                        ReplicatedStorage.rEvents.orbEvent:FireServer("collectOrb", selectedOrb, "City")
                    end
                    task.wait(0.001)
                end
            end)
        end
    end
})

local hoopConnection
MainFarm:AddToggle("AutoHoop", {
    Title = "⬆️ Auto Hoop",
    Default = false,
    Callback = function(state)
        if state then
            hoopConnection = RunService.RenderStepped:Connect(function()
                local root = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
                local hoops = game.Workspace:FindFirstChild("Hoops")
                if root and hoops then
                    for _, hoop in ipairs(hoops:GetChildren()) do
                        if hoop.Name == "Hoop" or hoop:FindFirstChild("Apperance") then
                            hoop.CFrame = root.CFrame
                            ReplicatedStorage.rEvents.hoopEvent:FireServer("finishedHoop")
                        end
                    end
                end
            end)
        else
            if hoopConnection then hoopConnection:Disconnect() hoopConnection = nil end
        end
    end
})

MainFarm:AddToggle("AutoRace", {
    Title = "🏁 Auto Race/ Win",
    Default = false,
    Callback = function(state)
        _G.autoRace = state
        if _G.autoRace then
            task.spawn(function()
                while _G.autoRace do
                    task.wait(0.5)
                    pcall(function()
                        if player.PlayerGui.gameGui.raceJoinLabel.Visible then
                            ReplicatedStorage.rEvents.raceEvent:FireServer("joinRace")
                        end
                        local raceMaps = game.Workspace:FindFirstChild("raceMaps")
                        if raceMaps and player.currentMap.Value ~= "" then
                            for _, map in ipairs(raceMaps:GetChildren()) do
                                if player.currentMap.Value == map.mapValue.Value then
                                    repeat task.wait() until player.PlayerGui.gameGui.countdownLabels.goLabel.Visible or not _G.autoRace
                                    if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                                        player.Character.HumanoidRootPart.CFrame = map.finishPart.CFrame
                                    end
                                end
                            end
                        end
                    end)
                end
            end)
        end
    end
})

local targetRebirthValue = 0
local infiniteToggle
local targetToggle

RebirthTab:AddInput("RebirthTarget", {
    Title = "🔢 Rebirth Target",
    Default = "",
    Numeric = true,
    Finished = true,
    Callback = function(text)
        targetRebirthValue = tonumber(text) or 0
    end
})

targetToggle = RebirthTab:AddToggle("TargetRebirth", {
    Title = "🔄 Auto Rebirth Target",
    Default = false,
    Callback = function(bool)
        _G.targetRebirthActive = bool
        if bool then
            if _G.infiniteRebirthActive then infiniteToggle:SetValue(false) end
            task.spawn(function()
                while _G.targetRebirthActive do
                    task.wait(0.1)
                    if player.leaderstats.Rebirths.Value >= targetRebirthValue then
                        targetToggle:SetValue(false)
                        break
                    end
                    ReplicatedStorage.rEvents.rebirthEvent:FireServer("rebirthRequest")
                end
            end)
        end
    end
})

infiniteToggle = RebirthTab:AddToggle("InfiniteRebirth", {
    Title = "♾️ Auto Rebirth (Infinitely)",
    Default = false,
    Callback = function(bool)
        _G.infiniteRebirthActive = bool
        if bool then
            if _G.targetRebirthActive then targetToggle:SetValue(false) end
            task.spawn(function()
                while _G.infiniteRebirthActive do
                    task.wait(0.1)
                    ReplicatedStorage.rEvents.rebirthEvent:FireServer("rebirthRequest")
                end
            end)
        end
    end
})

local ReplicatedStorage = game:GetService("ReplicatedStorage")

PetsTab:AddSection("💰 Auto Buy")

local selectedCrystal = "Blue Crystal"
local crystalDropdown = PetsTab:AddDropdown("CrystalDropdown", {
    Title = "💎 Select Crystal",
    Values = {"Blue Crystal", "Red Crystal", "Lightning Crystal", "Inferno Crystal", "Lava Crystal", "Snow Crystal", "Electro Crystal", "Desert Crystal"},
    Default = "Blue Crystal",
    Callback = function(Value)
        selectedCrystal = Value
    end
})

PetsTab:AddToggle("AutoHatchCryToggle", {
    Title = "🥚 Auto Open Crystal",
    Default = false,
    Callback = function(bool)
        _G.AutoHatchCry = bool
        if bool then
            task.spawn(function()
                while _G.AutoHatchCry do
                    local crystalToOpen = ReplicatedStorage.openCrystalsFolder:FindFirstChild(selectedCrystal)
                    if crystalToOpen then
                        ReplicatedStorage.rEvents.openCrystalRemote:InvokeServer("openCrystal", crystalToOpen.Name)
                    end
                    task.wait(0.1)
                end
            end)
        end
    end
})

local selectedPet = "Swift Samurai"
local petDropdown = PetsTab:AddDropdown("PetDropdown", {
    Title = "🐢 Select Pet",
    Values = {},
    Default = "Swift Samurai",
    Callback = function(Value)
        selectedPet = Value
    end
})

local petList = {"None", "Red Bunny", "Red Kitty", "Green Vampy", "Blue Bunny", "Dark Golem", "Silver Dog", "Pink Butterfly", "Purple Pegasus", "Yellow Squeak", "Yellow Butterfly", "Green Golem", "Golden Angel", "Orange Pegasus", "Golden Pheonix", "Orange Falcon", "Green Firecaster", "Blue Firecaster", "White Pheonix", "Red Pheonix", "Red Firecaster", "Flaming Hedgehog", "Quantum Dragon", "Tundra Dragon", "Magic Butterfly", "Soul Fusion Dog", "Hypersonic Pegasus", "Dark Soul Birdie", "Eternal Nebula Dragon", "Shadows Edge Kitty", "Ultimate Overdrive Bunny", "Blue Pheonix", "Magical Pegasus", "Voltaic Falcon", "Electro Golem", "Maestro Dog", "Divine Pegasus", "Golden Viking", "Speedy Sensei", "Swift Samurai"}
for _, name in ipairs(petList) do petDropdown:Add(name) end

PetsTab:AddToggle("AutoHatchPetToggle", {
    Title = "🐾 Auto Open Pet",
    Default = false,
    Callback = function(bool)
        _G.AutoHatchPet = bool
        if bool then
            task.spawn(function()
                while _G.AutoHatchPet and selectedPet ~= "" do
                    local petFolder = ReplicatedStorage:FindFirstChild("cPetShopFolder")
                    local petToOpen = petFolder and petFolder:FindFirstChild(selectedPet)
                    if petToOpen then
                        ReplicatedStorage.cPetShopRemote:InvokeServer(petToOpen)
                    end
                    task.wait(0.1)
                end
            end)
        end
    end
})

local selectedAura = "Ultimate"
local auraDropdown = PetsTab:AddDropdown("AuraDropdown", {
    Title = "✨ Select Aura",
    Values = {},
    Default = "Ultimate",
    Callback = function(Value)
        selectedAura = Value
    end
})

local auraList = {"None", "Blue City", "Yellow Lightning", "Green Energy", "Red Strike", "Purple Haze", "Orbit", "Magma", "Ice", "Electric", "Dark Matter", "Galaxy", "Soul", "Hyperspeed", "Rainbow", "Holy", "Chaos", "Ultimate"}
for _, name in ipairs(auraList) do auraDropdown:Add(name) end

PetsTab:AddToggle("AutoHatchAuraToggle", {
    Title = "🔥 Auto Open Aura",
    Default = false,
    Callback = function(bool)
        _G.AutoHatchAura = bool
        if bool then
            task.spawn(function()
                while _G.AutoHatchAura do
                    local auraToOpen = ReplicatedStorage:FindFirstChild("openCrystalsFolder") and ReplicatedStorage.openCrystalsFolder:FindFirstChild(selectedAura)
                    if auraToOpen then
                        ReplicatedStorage.rEvents.openCrystalRemote:InvokeServer("openCrystal", auraToOpen.Name)
                    end
                    task.wait(0.1)
                end
            end)
        end
    end
})

Misc:AddToggle("AntiAfkToggle", {
    Title = "⏳ Anti Afk",
    Default = false,
    Callback = function(Value)
        if Value then
            _G.AntiAfkActive = true
            
            local vu = game:GetService("VirtualUser")
            antiAfkConnection = game:GetService("Players").LocalPlayer.Idled:Connect(function()
                if _G.AntiAfkActive then
                    vu:CaptureController()
                    vu:ClickButton2(Vector2.new())
                end
            end)

            local ScreenGui = Instance.new("ScreenGui")
            local MainFrame = Instance.new("Frame")
            local UICorner = Instance.new("UICorner")
            local Title = Instance.new("TextLabel")
            local Separator = Instance.new("Frame")
            local TimerLabel = Instance.new("TextLabel")
            local FPSLabel = Instance.new("TextLabel")
            local MSLabel = Instance.new("TextLabel")
            local UIStroke = Instance.new("UIStroke")

            ScreenGui.Name = "Syniox_AntiAFK"
            ScreenGui.Parent = game:GetService("CoreGui")
            
            MainFrame.Parent = ScreenGui
            MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0) 
            MainFrame.BackgroundTransparency = 0.1
            MainFrame.Position = UDim2.new(0.05, 0, 0.4, 0)
            MainFrame.Size = UDim2.new(0, 120, 0, 125)
            MainFrame.Active = true
            MainFrame.Draggable = true 

            UICorner.CornerRadius = UDim.new(0, 8)
            UICorner.Parent = MainFrame

            UIStroke.Parent = MainFrame
            UIStroke.Color = Color3.fromRGB(150, 0, 0) 
            UIStroke.Thickness = 1.5

            Title.Parent = MainFrame
            Title.Size = UDim2.new(1, 0, 0, 28)
            Title.Text = "Syniox Anti-Afk" 
            Title.TextColor3 = Color3.fromRGB(200, 0, 0)
            Title.BackgroundTransparency = 1
            Title.TextSize = 15
            Title.Font = Enum.Font.SourceSansBold

            Separator.Parent = MainFrame
            Separator.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
            Separator.BorderSizePixel = 0
            Separator.Position = UDim2.new(0.1, 0, 0, 28)
            Separator.Size = UDim2.new(0.8, 0, 0, 1)

            local function SetupLabel(lbl, text, pos)
                lbl.Parent = MainFrame
                lbl.Position = pos
                lbl.Size = UDim2.new(1, 0, 0, 25)
                lbl.Text = text
                lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
                lbl.BackgroundTransparency = 1
                lbl.TextSize = 16
                lbl.Font = Enum.Font.SourceSansBold
                lbl.TextXAlignment = Enum.TextXAlignment.Center
            end

            SetupLabel(TimerLabel, "⏳ Time: 00:00:00", UDim2.new(0, 0, 0, 35))
            SetupLabel(FPSLabel, "🚀 FPS: 0", UDim2.new(0, 0, 0, 62))
            SetupLabel(MSLabel, "📡 MS: 0", UDim2.new(0, 0, 0, 89))

            local startTime = os.time()
            local RunService = game:GetService("RunService")
            local Stats = game:GetService("Stats")
            
            task.spawn(function()
                while _G.AntiAfkActive do
                    local diff = os.time() - startTime
                    TimerLabel.Text = string.format("⏳ Time: %02d:%02d:%02d", math.floor(diff/3600), math.floor((diff%3600)/60), diff%60)
                    
                    local fps = math.floor(1 / RunService.RenderStepped:Wait())
                    FPSLabel.Text = "🚀 FPS: " .. fps
                    
                    local ping = math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue())
                    MSLabel.Text = "📡 MS: " .. ping
                    
                    task.wait(0.5)
                end
            end)
        else
            _G.AntiAfkActive = false
            if antiAfkConnection then antiAfkConnection:Disconnect() end
            local oldGui = game:GetService("CoreGui"):FindFirstChild("Syniox_AntiAFK")
            if oldGui then oldGui:Destroy() end
        end
    end
})

Misc:AddToggle("LockPositionToggle", {
    Title = "🔒 Lock Position",
    Default = false,
    Callback = function(Value)
        if Value then
            local currentPos = player.Character.HumanoidRootPart.CFrame
            getgenv().posLock = RunService.Heartbeat:Connect(function()
                if player.Character:FindFirstChild("HumanoidRootPart") then player.Character.HumanoidRootPart.CFrame = currentPos end
            end)
        else
            if getgenv().posLock then getgenv().posLock:Disconnect() getgenv().posLock = nil end
        end
    end
})

Misc:AddToggle("AutoFortuneWheelToggle", {
    Title = "🎰 Auto Fortune Wheel",
    Default = false,
    Callback = function(Value)
        _G.autoFortuneWheelActive = Value
        if Value then
            task.spawn(function()
                while _G.autoFortuneWheelActive do
                    ReplicatedStorage.rEvents.openFortuneWheelRemote:InvokeServer("openFortuneWheel", ReplicatedStorage.fortuneWheelChances["Fortune Wheel"])
                    task.wait()
                end
            end)
        end
    end
})

Misc:AddToggle("NoClipToggle", {
    Title = "👻 No Clip",
    Default = false,
    Callback = function(bool)
        _G.noclipEnabled = bool
        if bool then
            RunService.Stepped:Connect(function()
                if _G.noclipEnabled and player.Character then
                    for _, part in pairs(player.Character:GetDescendants()) do
                        if part:IsA("BasePart") then part.CanCollide = false end
                    end
                end
            end)
        end
    end
})
