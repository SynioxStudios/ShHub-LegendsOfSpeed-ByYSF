local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/SynioxStudios/ByYusufGui-Syniox/refs/heads/main/SynioxGui.txt", true))()
local displayName = game.Players.LocalPlayer.DisplayName

local window = library:AddWindow("Syniox Hub | Legends Of Speed || HELLO - ".. displayName, {
    title_bar = {
        Color3.fromRGB(160, 0, 0), 
        Color3.fromRGB(40, 0, 0),  
        Color3.fromRGB(0, 0, 0)    
    }, 
    title_bar_transparency = 0.1, 
    background = {
        Color3.fromRGB(0, 0, 0), 
        Color3.fromRGB(10, 10, 10), 
        Color3.fromRGB(20, 20, 20)
    }, 
    background_transparency = 0.1, 
    main_color = Color3.fromRGB(160, 0, 0), 
    min_size = Vector2.new(450, 450), 
    can_resize = true 
})

local farm = window:AddTab("Farm")
farm:AddLabel("Fast Farm")

local selectedOrb = ""
local farming = false

local orbDropdown = farm:AddDropdown("Select Orb", function(text)
    selectedOrb = text
end)

local orbList = {
    "Yellow Orb", "Red Orb", "Blue Orb", "Orange Orb", "Ethereal Orb", "Gem"
}

for _, name in ipairs(orbList) do 
    orbDropdown:Add(name) 
end

farm:AddSwitch("Auto Farm Orbs", function(state)
    farming = state
    if farming then
        task.spawn(function()
            while farming do
                if selectedOrb ~= "" then
                    game:GetService("ReplicatedStorage").rEvents.orbEvent:FireServer("collectOrb", selectedOrb, "City")
                end
                task.wait(0.001)
            end
        end)
    end
end)

local hoopConnection

farm:AddSwitch("⬆️ Auto Hoop", function(state)
    local hooping = state
    
    if hooping then
        hoopConnection = game:GetService("RunService").RenderStepped:Connect(function()
            local root = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            local hoops = game.Workspace:FindFirstChild("Hoops")
            
            if root and hoops then
                for _, hoop in ipairs(hoops:GetChildren()) do
                    if hoop.Name == "Hoop" or hoop:FindFirstChild("Apperance") then
                        hoop.CFrame = root.CFrame
                        game:GetService("ReplicatedStorage").rEvents.hoopEvent:FireServer("finishedHoop")
                    end
                end
            end
        end)
    else
        if hoopConnection then
            hoopConnection:Disconnect()
            hoopConnection = nil
        end
    end
end)

local autoRace = false

farm:AddSwitch("🏁 Auto Race/ Win", function(state)
    autoRace = state
end)

task.spawn(function()
    while true do
        task.wait(0.5)
        if autoRace then
            pcall(function()
                local player = game.Players.LocalPlayer
                local rEvents = game:GetService("ReplicatedStorage").rEvents
                
                if player.PlayerGui.gameGui.raceJoinLabel.Visible then
                    rEvents.raceEvent:FireServer("joinRace")
                end

                local raceMaps = game.Workspace:FindFirstChild("raceMaps")
                if raceMaps and player.currentMap.Value ~= "" then
                    for _, map in ipairs(raceMaps:GetChildren()) do
                        if player.currentMap.Value == map.mapValue.Value then
                            repeat
                                task.wait()
                            until player.PlayerGui.gameGui.countdownLabels.goLabel.Visible or not autoRace

                            if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                                player.Character.HumanoidRootPart.CFrame = map.finishPart.CFrame
                            end
                        end
                    end
                end
            end)
        end
    end
end)

local rebirths = window:AddTab("Rebirths")
local targetRebirthValue = 0
local infiniteSwitch
local targetSwitch

rebirths:AddTextBox("🔢 Rebirth Target", function(text)
    local newValue = tonumber(text)
    if newValue and newValue > 0 then
        targetRebirthValue = newValue
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Updated Goal",
            Text = "New goal: " .. tostring(targetRebirthValue) .. " rebirths",
            Duration = 3
        })
    else
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Size",
            Text = "Put a size larger than 0",
            Duration = 3
        })
    end
end)

targetSwitch = rebirths:AddSwitch("🔄 Auto Rebirth Target", function(bool)
    _G.targetRebirthActive = bool
    if bool then
        if _G.infiniteRebirthActive and infiniteSwitch then
            infiniteSwitch:Set(false)
            _G.infiniteRebirthActive = false
        end
        spawn(function()
            while _G.targetRebirthActive and wait(0.1) do
                local currentRebirths = game.Players.LocalPlayer.leaderstats.Rebirths.Value
                if currentRebirths >= targetRebirthValue then
                    targetSwitch:Set(false)
                    _G.targetRebirthActive = false
                    game:GetService("StarterGui"):SetCore("SendNotification", {
                        Title = "Goal Reached",
                        Text = "You Have Reached " .. tostring(targetRebirthValue) .. " rebirths",
                        Duration = 5
                    })
                    break
                end
                game:GetService("ReplicatedStorage").rEvents.rebirthEvent:FireServer("rebirthRequest")
            end
        end)
    end
end)

infiniteSwitch = rebirths:AddSwitch("♾️ Auto Rebirth (Infinitely)", function(bool)
    _G.infiniteRebirthActive = bool
    if bool then
        if _G.targetRebirthActive and targetSwitch then
            targetSwitch:Set(false)
            _G.targetRebirthActive = false
        end
        spawn(function()
            while _G.infiniteRebirthActive and wait(0.1) do
                game:GetService("ReplicatedStorage").rEvents.rebirthEvent:FireServer("rebirthRequest")
            end
        end)
    end
end)

local shopTab = window:AddTab("Pet & Aura")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local selectedCrystal = "Blue Crystal"
local crystalDropdown = shopTab:AddDropdown("💎 Select Crystal", function(val)
    selectedCrystal = val
end)

crystalDropdown:Add("Blue Crystal")
crystalDropdown:Add("Red Crystal")
crystalDropdown:Add("Lightning Crystal")
crystalDropdown:Add("Inferno Crystal")
crystalDropdown:Add("Lava Crystal")
crystalDropdown:Add("Snow Crystal")
crystalDropdown:Add("Electro Crystal")
crystalDropdown:Add("Desert Crystal")

shopTab:AddSwitch("🥚 Auto Open Crystal", function(state)
    _G.AutoCrystal = state
    if state then
        spawn(function()
            while _G.AutoCrystal and selectedCrystal ~= "" do
                local crystalRemote = ReplicatedStorage.rEvents.openCrystalRemote
                if crystalRemote then
                    crystalRemote:InvokeServer("openCrystal", selectedCrystal)
                end
                task.wait(0.1)
            end
        end)
    end
end)

local selectedPet = ""
local petDropdown = shopTab:AddDropdown("🐢 Select Pet", function(val)
    selectedPet = val
end)

petDropdown:Add("None")
petDropdown:Add("Red Bunny")
petDropdown:Add("Red Kitty")
petDropdown:Add("Green Vampy")
petDropdown:Add("Blue Bunny")
petDropdown:Add("Dark Golem")
petDropdown:Add("Silver Dog")
petDropdown:Add("Pink Butterfly")
petDropdown:Add("Purple Pegasus")
petDropdown:Add("Yellow Squeak")
petDropdown:Add("Yellow Butterfly")
petDropdown:Add("Green Golem")
petDropdown:Add("Golden Angel")
petDropdown:Add("Orange Pegasus")
petDropdown:Add("Golden Pheonix")
petDropdown:Add("Orange Falcon")
petDropdown:Add("Green Firecaster")
petDropdown:Add("Blue Firecaster")
petDropdown:Add("White Pheonix")
petDropdown:Add("Red Pheonix")
petDropdown:Add("Red Firecaster")
petDropdown:Add("Flaming Hedgehog")
petDropdown:Add("Quantum Dragon")
petDropdown:Add("Tundra Dragon")
petDropdown:Add("Magic Butterfly")
petDropdown:Add("Soul Fusion Dog")
petDropdown:Add("Hypersonic Pegasus")
petDropdown:Add("Dark Soul Birdie")
petDropdown:Add("Eternal Nebula Dragon")
petDropdown:Add("Shadows Edge Kitty")
petDropdown:Add("Ultimate Overdrive Bunny")
petDropdown:Add("Blue Pheonix")
petDropdown:Add("Magical Pegasus")
petDropdown:Add("Voltaic Falcon")
petDropdown:Add("Electro Golem")
petDropdown:Add("Maestro Dog")
petDropdown:Add("Divine Pegasus")
petDropdown:Add("Golden Viking")
petDropdown:Add("Speedy Sensei")
petDropdown:Add("Swift Samurai")

shopTab:AddSwitch("💎 Auto Buy Pet", function(state)
    _G.AutoBuyPet = state
    if state then
        spawn(function()
            while _G.AutoBuyPet and selectedPet ~= "" and selectedPet ~= "None" do
                local petRemote = ReplicatedStorage.rEvents.openCrystalRemote
                if petRemote then
                    petRemote:InvokeServer("openCrystal", selectedPet)
                end
                task.wait(0.1)
            end
        end)
    end
end)

local selectedAura = ""
local auraDropdown = shopTab:AddDropdown("✨ Select Aura", function(val)
    selectedAura = val
end)

auraDropdown:Add("None")
auraDropdown:Add("Blue City")
auraDropdown:Add("Yellow Lightning")
auraDropdown:Add("Green Energy")
auraDropdown:Add("Red Strike")
auraDropdown:Add("Purple Haze")
auraDropdown:Add("Orbit")
auraDropdown:Add("Magma")
auraDropdown:Add("Ice")
auraDropdown:Add("Electric")
auraDropdown:Add("Dark Matter")
auraDropdown:Add("Galaxy")
auraDropdown:Add("Soul")
auraDropdown:Add("Hyperspeed")
auraDropdown:Add("Rainbow")
auraDropdown:Add("Holy")
auraDropdown:Add("Chaos")
auraDropdown:Add("Ultimate")

shopTab:AddSwitch("🔥 Auto Buy Aura", function(state)
    _G.AutoBuyAura = state
    if state then
        spawn(function()
            while _G.AutoBuyAura and selectedAura ~= "" and selectedAura ~= "None" do
                local auraRemote = ReplicatedStorage.rEvents.openCrystalRemote
                if auraRemote then
                    auraRemote:InvokeServer("openCrystal", selectedAura)
                end
                task.wait(0.1)
            end
        end)
    end
end)

local Misc = window:AddTab("Misc")

Misc:AddSwitch("⏳ Anti Afk", function(Value)
    if Value then
        _G.AntiAfkActive = true
        
        local vu = game:GetService("VirtualUser")
        antiAfkConnection = game:GetService("Players").LocalPlayer.Idled:Connect(function()
            if _G.AntiAfkActive then
                vu:CaptureController()
                vu:ClickButton2(Vector2.new())
            end
        end)

        local ScreenGui = Instance.new("ScreenGui")
        local MainFrame = Instance.new("Frame")
        local UICorner = Instance.new("UICorner")
        local Title = Instance.new("TextLabel")
        local Separator = Instance.new("Frame")
        local TimerLabel = Instance.new("TextLabel")
        local FPSLabel = Instance.new("TextLabel")
        local MSLabel = Instance.new("TextLabel")
        local UIStroke = Instance.new("UIStroke")

        ScreenGui.Name = "Syniox_AntiAFK"
        ScreenGui.Parent = game:GetService("CoreGui")
        
        MainFrame.Parent = ScreenGui
        MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0) 
        MainFrame.BackgroundTransparency = 0.1
        MainFrame.Position = UDim2.new(0.05, 0, 0.4, 0)
        MainFrame.Size = UDim2.new(0, 120, 0, 125)
        MainFrame.Active = true
        MainFrame.Draggable = true 

        UICorner.CornerRadius = UDim.new(0, 8)
        UICorner.Parent = MainFrame

        UIStroke.Parent = MainFrame
        UIStroke.Color = Color3.fromRGB(150, 0, 0) 
        UIStroke.Thickness = 1.5

        Title.Parent = MainFrame
        Title.Size = UDim2.new(1, 0, 0, 28)
        Title.Text = "Syniox Anti-Afk" 
        Title.TextColor3 = Color3.fromRGB(200, 0, 0)
        Title.BackgroundTransparency = 1
        Title.TextSize = 15
        Title.Font = Enum.Font.SourceSansBold

        Separator.Parent = MainFrame
        Separator.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
        Separator.BorderSizePixel = 0
        Separator.Position = UDim2.new(0.1, 0, 0, 28)
        Separator.Size = UDim2.new(0.8, 0, 0, 1)

        local function SetupLabel(lbl, text, pos)
            lbl.Parent = MainFrame
            lbl.Position = pos
            lbl.Size = UDim2.new(1, 0, 0, 25)
            lbl.Text = text
            lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
            lbl.BackgroundTransparency = 1
            lbl.TextSize = 16
            lbl.Font = Enum.Font.SourceSansBold
            lbl.TextXAlignment = Enum.TextXAlignment.Center
        end

        SetupLabel(TimerLabel, "⏳ Time: 00:00:00", UDim2.new(0, 0, 0, 35))
        SetupLabel(FPSLabel, "🚀 FPS: 0", UDim2.new(0, 0, 0, 62))
        SetupLabel(MSLabel, "📡 MS: 0", UDim2.new(0, 0, 0, 89))

        local startTime = os.time()
        local RunService = game:GetService("RunService")
        local Stats = game:GetService("Stats")
        
        task.spawn(function()
            while _G.AntiAfkActive do
                local diff = os.time() - startTime
                TimerLabel.Text = string.format("⏳ Time: %02d:%02d:%02d", math.floor(diff/3600), math.floor((diff%3600)/60), diff%60)
                
                local fps = math.floor(1 / RunService.RenderStepped:Wait())
                FPSLabel.Text = "🚀 FPS: " .. fps
                
                local ping = math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue())
                MSLabel.Text = "📡 MS: " .. ping
                
                task.wait(0.5)
            end
        end)

    else
        _G.AntiAfkActive = false
        if antiAfkConnection then antiAfkConnection:Disconnect() end
        local oldGui = game:GetService("CoreGui"):FindFirstChild("Syniox_AntiAFK")
        if oldGui then oldGui:Destroy() end
    end
end)

local switch = Misc:AddSwitch("🔒 Lock Position", function(Value)
    if Value then
        
        local currentPos = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
        getgenv().posLock = game:GetService("RunService").Heartbeat:Connect(function()
            if game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = currentPos
            end
        end)
    else
        
        if getgenv().posLock then
            getgenv().posLock:Disconnect()
            getgenv().posLock = nil
        end
    end
end)

Misc:AddSwitch("🎰 Auto Fortune Wheel", function(Value)
    _G.autoFortuneWheelActive = Value
    if Value then
        local function openFortuneWheel()
            while _G.autoFortuneWheelActive do
                local args = {
                    [1] = "openFortuneWheel",
                    [2] = game:GetService("ReplicatedStorage"):WaitForChild("fortuneWheelChances"):WaitForChild("Fortune Wheel")
                }
                game:GetService("ReplicatedStorage"):WaitForChild("rEvents"):WaitForChild("openFortuneWheelRemote"):InvokeServer(unpack(args))
                wait(0)
            end
        end
        coroutine.wrap(openFortuneWheel)()
    else
        _G.autoFortuneWheelActive = false
    end
end)

Misc:AddButton("🔄 Rejoin Game", function()
    local ScreenGui = Instance.new("ScreenGui")
    local Frame = Instance.new("Frame")
    local TextLabel = Instance.new("TextLabel")
    local YesButton = Instance.new("TextButton")
    local NoButton = Instance.new("TextButton")
    local UICorner = Instance.new("UICorner")

    ScreenGui.Parent = game:GetService("CoreGui")
    ScreenGui.Name = "RejoinConfirm"

    Frame.Name = "MainFrame"
    Frame.Parent = ScreenGui
    Frame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    Frame.Position = UDim2.new(0.5, -125, 0.5, -75)
    Frame.Size = UDim2.new(0, 250, 0, 150)
    Frame.BorderSizePixel = 0
    Frame.Active = true
    Frame.Draggable = true 
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 10)

    TextLabel.Parent = Frame
    TextLabel.BackgroundTransparency = 1
    TextLabel.Position = UDim2.new(0, 10, 0, 20)
    TextLabel.Size = UDim2.new(0, 230, 0, 50)
    TextLabel.Font = Enum.Font.GothamBold
    TextLabel.Text = "Are you sure you want to rejoin?"
    TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    TextLabel.TextSize = 16
    TextLabel.TextWrapped = true

    YesButton.Name = "YesButton"
    YesButton.Parent = Frame
    YesButton.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
    YesButton.Position = UDim2.new(0, 25, 0, 90)
    YesButton.Size = UDim2.new(0, 85, 0, 35)
    YesButton.Font = Enum.Font.GothamBold
    YesButton.Text = "YES"
    YesButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    YesButton.TextSize = 14
    Instance.new("UICorner", YesButton).CornerRadius = UDim.new(0, 6)

    NoButton.Name = "NoButton"
    NoButton.Parent = Frame
    NoButton.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
    NoButton.Position = UDim2.new(0, 140, 0, 90)
    NoButton.Size = UDim2.new(0, 85, 0, 35)
    NoButton.Font = Enum.Font.GothamBold
    NoButton.Text = "NO"
    NoButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    NoButton.TextSize = 14
    Instance.new("UICorner", NoButton).CornerRadius = UDim.new(0, 6)

    YesButton.MouseButton1Click:Connect(function()
        game:GetService("TeleportService"):Teleport(game.PlaceId, game.Players.LocalPlayer)
    end)

    NoButton.MouseButton1Click:Connect(function()
        ScreenGui:Destroy()
    end)
end)

Misc:AddSwitch("👻 No Clip", function(bool)
    noclipEnabled = bool
    
    if noclipEnabled then
        startNoclip()
    else
        if noclipConnection then
            noclipConnection:Disconnect()
            noclipConnection = nil
        end
        
        if player.Character then
            for _, part in pairs(player.Character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = true
                end
            end
        end
    end
end)

