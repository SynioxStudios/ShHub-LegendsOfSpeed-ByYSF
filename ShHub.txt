local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/SynioxStudios/ByYusufGui-Syniox/refs/heads/main/SynioxGui.txt", true))()
local displayName = game.Players.LocalPlayer.DisplayName

local window = library:AddWindow("Syniox Hub | Legends Of Speed || HELLO - ".. displayName, {
    title_bar = {
        Color3.fromRGB(160, 0, 0), 
        Color3.fromRGB(40, 0, 0),  
        Color3.fromRGB(0, 0, 0)    
    }, 
    title_bar_transparency = 0.1, 
    background = {
        Color3.fromRGB(0, 0, 0), 
        Color3.fromRGB(10, 10, 10), 
        Color3.fromRGB(20, 20, 20)
    }, 
    background_transparency = 0.1, 
    main_color = Color3.fromRGB(160, 0, 0), 
    min_size = Vector2.new(450, 450), 
    can_resize = true 
})

local farm = window:AddTab("Farm")
farm:AddLabel("Fast Farm")

local selectedOrb = ""
local farming = false

local orbDropdown = farm:AddDropdown("Select Orb", function(text)
    selectedOrb = text
end)

local orbList = {
    "Yellow Orb", "Red Orb", "Blue Orb", "Orange Orb", "Ethereal Orb"
}

for _, name in ipairs(orbList) do 
    orbDropdown:Add(name) 
end

farm:AddSwitch("Auto Farm Orbs", function(state)
    farming = state
    if farming then
        task.spawn(function()
            while farming do
                if selectedOrb ~= "" then
                    game:GetService("ReplicatedStorage").rEvents.orbEvent:FireServer("collectOrb", selectedOrb, "City")
                end
                task.wait(0.001)
            end
        end)
    end
end)

farm:AddSwitch("Auto Hoops", function(state)
    local hooping = state
    
    local hoopCoords = {
        Vector3.new(-86.84, 114.95, -111.44),
        Vector3.new(230.86, 91.99, 79.77),
        Vector3.new(-534.23, 57.67, 208.96),
        Vector3.new(-541.89, 59.97, -129.51)
    }

    if hooping then
        task.spawn(function()
            while hooping do
                for _, pos in ipairs(hoopCoords) do
                    if not hooping then break end
                    
                    -- Karakteri gerçekten hareket ettirmeden 
                    -- Sadece sunucuya giden paketi o koordinattanmış gibi yolluyoruz
                    pcall(function()
                        game:GetService("ReplicatedStorage").rEvents.hoopEvent:FireServer("finishedHoop", pos)
                    end)
                    
                    task.wait(0.05)
                end
                task.wait()
            end
        end)
    end
end)
